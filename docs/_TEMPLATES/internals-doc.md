# <Component Name>

Implementation: `internal/<package>/<file>.go`

---

## What it does

<!-- One paragraph: what problem does this component solve, where does it fit in the pipeline? -->

---

## Types

<!-- Show the key Go struct definitions. Explain each field's purpose. -->

```go
type Foo struct {
    Field string // what it means
}
```

---

## Key methods

<!-- Document each exported method: signature, what it does, what can go wrong. -->

### `MethodName(args) → result`

<!-- What it does, when to call it, what errors it returns. -->

---

## How it connects to the rest of the system

<!-- Which packages call into this one? Which packages does this one call into?
     A short diagram or paragraph is fine. -->
