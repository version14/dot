name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

permissions:
  contents: read

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  validate:
    uses: ./.github/workflows/validate.yml

  commitlint:
    uses: ./.github/workflows/commitlint.yml
    permissions:
      contents: read
      pull-requests: write

  dependency-review:
    if: github.event_name == 'pull_request'
    uses: ./.github/workflows/dependency-review.yml
    permissions:
      contents: read
      pull-requests: write

  dep-scan:
    if: github.event_name == 'pull_request'
    uses: ./.github/workflows/dep-scan.yml
    permissions:
      contents: read
      pull-requests: write

  # Rules 1 and 2 from ADR-0002: a Generator's Manifest Version moves when — and
  # only when — what it scaffolds changes, and its doc row never drifts from it.
  generator-versions:
    uses: ./.github/workflows/generator-versions.yml
    permissions:
      contents: read

  test:
    uses: ./.github/workflows/unit-tests.yml

  build:
    needs: [validate, test]
    uses: ./.github/workflows/build.yml

  test-flows:
    needs: [build]
    uses: ./.github/workflows/test-flows.yml
